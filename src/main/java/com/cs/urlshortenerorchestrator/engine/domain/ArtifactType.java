package com.cs.urlshortenerorchestrator.engine.domain;

/**
 * ArtifactType enum: different types of artifacts produced during workflow execution.
 */
public enum ArtifactType {
    CODE, SCHEMA, API_SPEC, TEST, DOCUMENTATION,
    REQUIREMENT_SPEC, ARCHITECTURE_PLAN, DEPLOYMENT_PLAN
}

